// External wiring only — DOM/Vue event listeners that call module.ts.
// No business logic here.

let attached = false

export const initIntegrations = (): void => {
    if (attached) {
        return
    }

    attached = true

    // TODO: wire DOM/Vue events to module functions
}